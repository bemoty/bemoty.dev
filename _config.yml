title: bemoty's website # name of the site
author: Bemoty # name of site's author
email: josh@bemoty.dev # email of site's author
url: https://bemoty.dev # root address of the site
baseurl: "" # subpath of the site, e.g. "/blog"
description: > # description of the site (multiple lines allowed)
  bemoty? bemoty? what's a bemoty?

highlighter: rouge
permalink: /:slug

favicon: "favicon.ico"

remote_theme: riggraz/no-style-please # if you are using GitHub Pages, change it to remote_theme: riggraz/no-style-please

theme_config:
  appearance: "auto" # can be "light", "dark" or "auto"
  back_home_text: "←" # customize text for homepage link in post layout
  date_format: "%Y-%m-%d" # customize how date is formatted
  show_description: false # show blog description in home page

sass:
  style: :compressed

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap